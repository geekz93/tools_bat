@echo off
echo -----------------------------------------------------
echo 注意：1.需要以管理员身份运行 2.需要关闭系统自动休眠功能
echo -----------------------------------------------------
set /p timeS=输入时刻(格式 xx:xx): 
at %timeS% shutdown -s -t 0
pause